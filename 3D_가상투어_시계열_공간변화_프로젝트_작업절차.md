# 3차원 가상투어 및 시계열 공간 변화 모니터링 프로젝트 작업 절차

## 1. 프로젝트 목표

실내 공간을 360° 카메라로 반복 촬영하고, 촬영 영상에서 위치·방향·이동 흐름 등의 공간 컨텍스트를 생성한 뒤 다음 기능을 구현한다.

- 촬영 영상 및 메타데이터 자동 생성
- 프레임 정렬 및 공간 정합
- 대표 키프레임 자동 선별
- 360° 가상투어 자동 생성
- 서로 다른 날짜의 동일 공간 비교
- AI 기반 객체·공간 변화·이벤트 탐지
- 시계열 공간 이력 관리 및 웹 시각화

> 정밀한 3D Mesh 생성 자체가 필수 목표라기보다는, 360° 영상과 공간 위치관계를 이용한 가상투어 및 시계열 변화 모니터링이 핵심이다.

---

## 2. 전체 파이프라인

```text
360° 실내 촬영
    ↓
영상 프레임 추출
    ↓
SLAM / Visual Odometry
    ↓
카메라 위치·방향 추정
    ↓
메타데이터 생성 및 저장
    ↓
키프레임 자동 선별
    ↓
360° 가상투어 생성
    ↓
다른 날짜의 촬영 데이터 입력
    ↓
두 촬영 세션 간 공간 정합
    ↓
동일 위치 Before / After 비교
    ↓
AI 기반 공간 변화 및 객체 탐지
    ↓
웹 기반 시각화 및 이력 관리
```

---

# 3. 세부 작업 절차

## Step 1. 360° 실내 공간 촬영

동일한 실내 공간을 360° 카메라로 이동하면서 촬영한다.

### 촬영 시 확보할 정보

- 영상
- 촬영 시간
- 촬영 세션 ID
- 카메라 방향
- 이동 경로
- 가능하면 IMU 데이터

실내에서는 GPS 오차가 크기 때문에 GPS를 주 위치 추정 수단으로 사용하지 않는다.

---

## Step 2. 영상 프레임 추출 및 전처리

360° 동영상에서 일정 간격으로 프레임을 추출한다.

예:

```text
video.mp4
 ├─ frame_000001.jpg
 ├─ frame_000002.jpg
 ├─ frame_000003.jpg
 └─ ...
```

### 수행 작업

- 프레임 추출
- 블러 프레임 제거
- 지나치게 유사한 프레임 제거
- 노출이 비정상적인 프레임 제거
- 필요 시 Equirectangular → Cubemap 변환

360° 원본 이미지는 왜곡이 크므로 특징점 매칭 시 Cubemap으로 변환하는 방식을 고려한다.

---

## Step 3. 실내 위치 및 자세 추정

실내 위치는 GPS 대신 Visual SLAM 또는 Visual-Inertial SLAM으로 추정한다.

### 기본 구조

```text
360° 영상
   +
IMU(가능한 경우)
   ↓
Visual Odometry
   ↓
SLAM
   ↓
카메라 Pose
```

각 키프레임에 다음 정보를 생성한다.

```json
{
  "frame_id": 152,
  "timestamp": 12.53,
  "position": [2.31, 0.42, 1.55],
  "rotation": [0.01, 0.03, 1.27],
  "session_id": "2026-09-13-A"
}
```

### 후보 기술

- ORB-SLAM3
- OpenVSLAM 계열
- COLMAP
- OpenCV 기반 Visual Odometry

실시간 처리가 필수가 아니라면 초기 프로토타입에서는 COLMAP 같은 오프라인 방식도 사용할 수 있다.

---

## Step 4. SLAM 맵 생성

촬영 세션별로 다음 데이터를 생성한다.

- 카메라 이동 경로
- 카메라 Pose
- Keyframe
- Sparse Point Cloud
- 특징점 Descriptor

예:

```text
Session A

Camera 1 ─ Camera 2 ─ Camera 3
                       │
                     Camera 4

        · · · · ·
     Sparse 3D Points
        · · · · ·
```

---

## Step 5. 키프레임 자동 선별

모든 영상 프레임을 가상투어에 사용하면 데이터가 지나치게 많아진다.

따라서 다음 기준으로 대표 키프레임을 선정한다.

- 일정 거리 이상 이동
- 일정 각도 이상 회전
- 이전 프레임과 시각적 차이가 충분함
- 주요 공간 진입
- 중복 프레임 제거

예:

```text
10000 Frames
     ↓
SLAM + 유사도 분석
     ↓
100~300 Keyframes
```

---

# 4. 가상투어 생성

키프레임과 카메라 위치를 이용해 공간 이동 그래프를 만든다.

```text
[입구]
   ↓
[복도 A] ─── [회의실]
   ↓
[복도 B]
   ↓
[사무실]
```

각 노드에는 360° 파노라마를 연결한다.

### 웹 구현 후보

- Three.js
- WebGL
- 360 Panorama Viewer 라이브러리

사용자는 노드 간 이동을 통해 실내 공간을 둘러볼 수 있다.

---

# 5. 다른 날짜의 공간 데이터 생성

동일 공간을 일정 시간이 지난 후 다시 촬영한다.

예:

```text
Session A
2026-09-01

Session B
2026-10-01
```

각 촬영 세션은 독립적으로 SLAM을 수행한다.

문제는 두 세션의 좌표계가 서로 다르다는 점이다.

```text
Session A 좌표계

A ─ B ─ C


Session B 좌표계

       A'
      /
    B'
   /
 C'
```

따라서 두 좌표계를 하나로 맞추는 **공간 정합** 과정이 필요하다.

---

# 6. 공간 정합

공간 정합의 목표는 서로 다른 촬영 세션에서 생성된 SLAM 맵을 동일 좌표계에 배치하는 것이다.

전체 과정은 다음과 같다.

```text
Visual Place Recognition
        ↓
Keyframe 후보 검색
        ↓
Feature Matching
        ↓
RANSAC
        ↓
SE(3) / Sim(3) Transform 추정
        ↓
ICP 미세 정합
        ↓
Pose Graph Optimization
```

---

## Step 6-1. 동일 장소 후보 찾기

두 촬영 세션에서 비슷한 공간을 촬영한 키프레임을 찾는다.

예:

```text
Session A Frame #120
        ↕
Session B Frame #183
```

방법:

- 이미지 임베딩
- Visual Place Recognition
- 이미지 특징점 기반 검색

---

## Step 6-2. Feature Matching

후보 프레임 사이에서 실제 특징점을 찾는다.

추천 후보:

- SIFT
- ORB
- SuperPoint
- LightGlue

예:

```text
Session A                  Session B

문 모서리 ● ───────────── ● 문 모서리
표지판    ● ───────────── ● 표지판
손잡이    ● ───────────── ● 손잡이
```

---

## Step 6-3. RANSAC을 통한 오매칭 제거

Feature Matching에는 잘못된 대응점이 포함될 수 있다.

RANSAC을 사용해 일관된 대응점만 선택한다.

```text
Matching Points
      ↓
RANSAC
      ↓
Inlier Matches
```

---

## Step 6-4. 좌표계 변환 계산

두 Map 사이의 Transformation을 계산한다.

Metric Scale이 확보된 경우:

```text
X_A = R X_B + t
```

- R: Rotation
- t: Translation

단안 SLAM 등으로 Scale이 확실하지 않은 경우:

```text
X_A = s R X_B + t
```

- s: Scale
- R: Rotation
- t: Translation

즉 SE(3) 또는 Sim(3) Transformation을 계산한다.

---

## Step 6-5. ICP 미세 정합

Sparse/Dense Point Cloud가 있다면 ICP를 이용해 정합 결과를 추가 보정한다.

```text
정합 전

Map A  ·······
Map B    ·······


정합 후

Map A  ·······
Map B  ·······
```

---

## Step 6-6. Pose Graph Optimization

한두 개의 대응점만 사용하지 않고 여러 공간 대응 관계를 사용해 전체 Map을 최적화한다.

예:

```text
A  ↔ A'
B  ↔ B'
C  ↔ C'
D  ↔ D'

     ↓

Pose Graph Optimization
```

이를 통해 특정 프레임의 오류가 전체 공간 정합을 망치는 것을 줄일 수 있다.

---

# 7. 동일 위치의 과거/현재 이미지 비교

공간 정합이 완료되면 동일 공간 위치에 해당하는 과거/현재 프레임을 찾을 수 있다.

예:

```text
Position: (5.2, 2.1, 1.5)

2026-09-01
     ↓
Before Image

2026-10-01
     ↓
After Image
```

---

# 8. 공간 변화 탐지

정합된 이미지끼리 변화 분석을 수행한다.

### 1차 구현

- Pixel Difference
- SSIM
- Edge Difference

### 고도화

- Object Detection
- Semantic Segmentation
- Instance Segmentation
- Feature-level Change Detection

검출 대상 예:

- 새로운 물체 등장
- 기존 물체 제거
- 물체 이동
- 가구 배치 변화
- 시설물 상태 변화
- 비정상 객체 또는 이벤트

---

# 9. 객체 단위 변화 분석

Before / After 각각에서 객체를 검출한다.

```text
Before

chair
desk
monitor


After

chair
desk
monitor
box
```

결과:

```text
box → 신규 객체
```

또는 위치를 비교하여:

```text
chair
Before: (3.2, 1.4)
After : (4.1, 1.4)

→ 위치 변경
```

### AI 모델 후보

- YOLO
- DETR 계열
- Mask R-CNN
- Segment Anything 계열

프로젝트 요구사항에 따라 기존 pretrained 모델을 활용하고 공간 변화 판정 로직에 집중할 수 있다.

---

# 10. 변화 영역 시각화

AI가 찾은 변화 영역을 360° 화면 위에 표시한다.

예:

```text
┌─────────────────────────┐
│                         │
│        [NEW BOX]        │
│          █████          │
│                         │
└─────────────────────────┘
```

표현 방법:

- Bounding Box
- Segmentation Mask
- Heatmap
- 변경 위치 Marker

---

# 11. 시계열 공간 이력 관리

각 공간의 촬영 결과를 날짜별로 저장한다.

```text
Building A
 ├─ Floor 1
 │   ├─ 2026-09-01
 │   ├─ 2026-09-15
 │   └─ 2026-10-01
 │
 └─ Floor 2
     ├─ 2026-09-01
     └─ 2026-10-01
```

각 시점에는 다음 정보를 저장한다.

- 영상
- 키프레임
- 카메라 Pose
- SLAM Map
- Point Cloud
- 공간 메타데이터
- 검출 객체
- 변화 이벤트

---

# 12. 웹 플랫폼 구현

최종적으로 사용자가 다음 기능을 사용할 수 있도록 한다.

### 공간 탐색

- 건물 선택
- 층 선택
- 공간 선택
- 360° 가상투어

### 시간 선택

```text
2026-09-01
2026-09-15
2026-10-01
```

### 변화 분석

- Before / After 비교
- 변화 객체 표시
- 변화 위치 표시
- 이벤트 목록 표시

### 관리 기능

- 촬영 이력 확인
- 변화 이력 검색
- 공간별 이벤트 기록 조회

---

# 13. 권장 기술 스택

## 영상/비전

- Python
- OpenCV
- NumPy

## SLAM / 공간 복원

- ORB-SLAM3
- COLMAP
- Open3D

## Feature Matching

- SIFT / ORB
- SuperPoint
- LightGlue

## AI

- PyTorch
- YOLO
- Segmentation 모델

## Backend

- FastAPI

## Frontend

- JavaScript / TypeScript
- Three.js
- WebGL

## Database

- PostgreSQL
- SQLite (초기 프로토타입)
- 파일 스토리지 + Metadata DB

---

# 14. 실제 개발 권장 순서

한 번에 전체 시스템을 구현하지 않고 아래 순서로 단계별 개발한다.

## Phase 1. 영상 처리

```text
360 영상
→ 프레임 추출
→ Cubemap 변환
```

## Phase 2. 위치 추정

```text
영상
→ SLAM
→ Camera Pose 생성
```

## Phase 3. 가상투어

```text
SLAM Pose
→ Keyframe 선정
→ 공간 그래프 생성
→ 360° Viewer
```

## Phase 4. 공간 정합

```text
Session A + Session B
→ Place Recognition
→ Feature Matching
→ RANSAC
→ SE(3) / Sim(3)
→ ICP
```

## Phase 5. 변화 탐지

```text
Before / After
→ Image Alignment
→ Object Detection
→ Change Detection
```

## Phase 6. 시계열 관리

```text
촬영 Session
→ DB 저장
→ 날짜별 비교
→ 변화 이력 생성
```

## Phase 7. 웹 통합

```text
가상투어
+
공간 변화 분석
+
시계열 데이터
↓
통합 모니터링 웹 플랫폼
```

---

# 15. 우선 구현해야 할 MVP

처음부터 완전한 3D 복원이나 고성능 AI까지 구현하기보다 다음 수준을 1차 목표로 한다.

### MVP 1

- 360 영상 입력
- 프레임 자동 추출
- SLAM을 통한 카메라 경로 생성
- 키프레임 자동 선택

### MVP 2

- 키프레임 기반 360° 가상투어

### MVP 3

- 같은 공간의 두 촬영 Session 정합
- 동일 위치 프레임 대응

### MVP 4

- Before / After 공간 변화 탐지

### MVP 5

- 웹에서 변화 위치와 촬영 이력 시각화

---

# 16. 최종 구조

```text
                 [360° Camera]
                       │
                       ▼
                영상 데이터 수집
                       │
                       ▼
                Frame Extraction
                       │
                       ▼
             Visual / VI-SLAM
                       │
            ┌──────────┴──────────┐
            ▼                     ▼
       Camera Pose          Sparse 3D Map
            │                     │
            └──────────┬──────────┘
                       ▼
               Keyframe Selection
                       │
                       ▼
                 Virtual Tour
                       │
                다른 날짜 촬영
                       │
                       ▼
               Spatial Alignment
                       │
             ┌─────────┴─────────┐
             ▼                   ▼
        Before Frame         After Frame
             └─────────┬─────────┘
                       ▼
                 Change Detection
                       │
                       ▼
              Object/Event Analysis
                       │
                       ▼
                Web Visualization
                       │
                       ▼
              Time-Series Monitoring
```

---

## 핵심 정리

이 프로젝트의 핵심 작업 흐름은 다음과 같다.

> **360° 실내 영상 촬영 → SLAM 기반 실내 상대 위치 추정 → 키프레임 및 공간 관계 생성 → 가상투어 자동 생성 → 서로 다른 날짜의 SLAM Map 공간 정합 → 동일 위치의 영상 비교 → AI 기반 공간 변화 탐지 → 웹 기반 시계열 모니터링**

실내 위치 추정에는 GPS 대신 **Visual SLAM / Visual-Inertial SLAM**을 사용하고, 서로 다른 날짜의 촬영 결과는 **Feature Matching + RANSAC + SE(3)/Sim(3) + ICP** 등을 통해 동일 좌표계로 정합하는 방향으로 구현한다.
